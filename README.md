# DHBW-Demo-CleanCode
Demo Clean Code

## SOLID
In every SOLID folder there is an example that violates the specific principle. The "solution" folder contains the same example, but fixed. The exercise folder contains a NEW example that violates the principle, but there is no solution for it. Creating the solution would be your task.
